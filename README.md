This is our django web server, for book management.

To get started, run:

python -m venv .venv
make sure you activate the virtual enviroment after running the previous command.
Then run:
pip install -r requirements.txt

This will setup your virutal env, and install the required packages.

To start the webserver, run:

python manage.py runserver